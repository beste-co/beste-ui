import type { ComponentMeta } from "@/lib/component-types";

export const meta: ComponentMeta = {
  name: "form28",
  title: "Autosave Field",
  description:
    "Inline field with an autosave status pill that shifts between saved, saving, and unsaved dot colors.",
  category: "Form",
};
