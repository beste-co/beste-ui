import type { ComponentMeta } from "@/lib/component-types";

export const meta: ComponentMeta = {
  name: "ai46",
  title: "Streaming Reply",
  description:
    "Chat exchange that thinks with bouncing dots, streams the answer character by character behind a blinking caret, then rests and plays again.",
  category: "AI",
};
