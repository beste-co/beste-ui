import type { ComponentMeta } from "@/lib/component-types";

export const meta: ComponentMeta = {
  name: "browser37",
  title: "Page Load",
  description:
    "Browser frame that loads on repeat: the progress line runs across the top, skeletons pulse in the body, then the real copy settles into place.",
  category: "Browser",
};
