/**
 * Site-wide configuration settings
 *
 * REQUIRE_LOGIN_FOR_FREE_BLOCKS:
 * When true, users must be logged in to view code and copy install commands for free blocks.
 * When false, free blocks are fully accessible without login (pro blocks still require login + license).
 */
export const REQUIRE_LOGIN_FOR_FREE_BLOCKS = false;
